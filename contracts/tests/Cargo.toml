[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
blend-pool-sim = { path = "../contracts/simulation/blend-pool-sim" }
escrow = { path = "../contracts/escrow" }
standardized_yield = { path = "../contracts/tokens/standardized_yield" }

[[test]]
name = "blend_pool_tests"
path = "blend_pool_tests.rs"

[[test]]
name = "escrow_tests"
path = "escrow_tests.rs"
