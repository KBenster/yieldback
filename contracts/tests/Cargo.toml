[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
yield-pool-sim = { path = "../contracts/simulation/yield-pool-sim" }
escrow = { path = "../contracts/escrow" }
standardized_yield = { path = "../contracts/tokens/standardized_yield" }

[[test]]
name = "yield_pool_tests"
path = "yield_pool_tests.rs"

[[test]]
name = "escrow_tests"
path = "escrow_tests.rs"
