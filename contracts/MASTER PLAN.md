Make the factory deploy the principal and yield tokens, then link them together.
the factory should be the only thing thats deploying any contracts
^double check that, ask claude if its good practice to follow that
